---
title: MSW (Mock Service Worker)
description: Panduan setup dan penggunaan MSW dalam konvensi Next.js Clean Architecture — API mocking untuk development paralel dan testing yang terisolasi dari backend.
---

## Tujuan dalam Konvensi Ini

MSW digunakan untuk **mengintercepti HTTP request di network layer** dan mengembalikan response dummy sesuai API contract — tanpa mengubah kode produksi sama sekali. Ini memungkinkan frontend developer bekerja paralel dengan backend developer selama API contract sudah disepakati.

Dalam konvensi ini, MSW terintegrasi langsung dengan layer `api/` melalui dua komponen:

- **`EP_` (Endpoint Registry)** — konstanta path API yang dipakai bersama oleh fungsi fetch (`APIS_`/`APIC_`) dan mock handler
- **`[resource].mock-handler.ts`** — handler MSW yang co-located dengan file API-nya

---

## Setup

```bash
npm install msw --save-dev
```

Setelah instalasi, buat Service Worker file untuk browser:

```bash
npx msw init public/ --save
```

### File Wiring di `src/mocks/`

Folder `src/mocks/` hanya berisi setup dan pengumpulan handler — **bukan** tempat mendefinisikan handler.

```ts
// src/mocks/browser.ts
import { setupWorker } from "msw/browser"
import { handlers } from "."

export const worker = setupWorker(...handlers)
```

```ts
// src/mocks/node.ts
import { setupServer } from "msw/node"
import { handlers } from "."

export const server = setupServer(...handlers)
```

`index.ts` adalah unified orchestrator — mendaftarkan semua handler, menyediakan `initMocksClient()` dan `initMocksServer()`, dan logging terpusat:

```ts
// src/mocks/index.ts

import { userListHandler } from "@/api/user-management/users-list.mock-handler"

export const handlers = [
    userListHandler,
    // tambah handler baru di sini setiap kali menambah mock
]

function attachLogger(instance: { events: any }, runtime: "BROWSER" | "SERVER") {
    instance.events.on("response:mocked", ({ request, response }: any) => {
        console.log(`[MSW][${runtime}] ${request.method} ${new URL(request.url).pathname} → ${response.status}`)
    })
    instance.events.on("request:unhandled", ({ request }: any) => {
        console.warn(`[MSW][${runtime}] ⚠ unhandled: ${request.method} ${new URL(request.url).pathname}`)
    })
}

// ─── Client (browser) ────────────────────────────────────────────────────────
let clientInitialized = false

export async function initMocksClient() {
    if (clientInitialized) return
    if (typeof window === "undefined") return
    const { worker } = await import("./browser")
//                          ↑ dynamic import — tidak di-bundle kecuali dipanggil
    attachLogger(worker, "BROWSER")
    await worker.start({ onUnhandledRequest: "bypass" })
    clientInitialized = true
//          ↑ singleton guard — mencegah double init jika dipanggil dari dua tempat
}

// ─── Server (Node.js) ────────────────────────────────────────────────────────
export async function initMocksServer() {
    if (process.env.NEXT_RUNTIME !== "nodejs") return
    const { server } = await import("./node")
//                          ↑ dynamic import — browser tidak akan bundle Node.js API
    attachLogger(server, "SERVER")
    server.listen({ onUnhandledRequest: "bypass" })
}
```

> Dynamic import di kedua fungsi ini penting: `msw/browser` menggunakan Service Worker API yang tidak ada di Node.js, dan `msw/node` menggunakan API Node.js yang tidak ada di browser. Tanpa dynamic import, bundler bisa salah include keduanya.

### Aktivasi — Primary (Next.js 15+)

Next.js 15 menyediakan `instrumentation-client.ts` (client) dan `instrumentation.ts` (server) sebagai titik aktivasi yang bersih — terpisah dari UI layer:

```ts
// src/instrumentation-client.ts
// Dieksekusi sekali di sisi client saat app pertama kali dimuat
import { initMocksClient } from "@/mocks"

if (process.env.NEXT_PUBLIC_API_MOCKING === "enabled") {
    await initMocksClient()
}
```

```ts
// src/instrumentation.ts
// Dieksekusi sekali di sisi server saat Next.js server start
export async function register() {
    if (process.env.NEXT_PUBLIC_API_MOCKING !== "enabled") return
    const { initMocksServer } = await import("@/mocks")
    await initMocksServer()
}
```

### Aktivasi — Fallback (`app/layout.tsx`)

Jika tidak ingin menggunakan instrumentation files, atau sebagai fallback tambahan, bisa aktifkan dari root layout. Singleton guard di `initMocksClient()` memastikan tidak terjadi double init:

```ts
// app/layout.tsx (atau app/[locale]/layout.tsx)
if (process.env.NEXT_PUBLIC_API_MOCKING === "enabled") {
    const { initMocksClient } = await import("@/mocks")
    await initMocksClient()
//          ↑ aman dipanggil dua kali — clientInitialized guard mencegah double init
}
```

Buat file `.env.local` dan tambahkan:

```
NEXT_PUBLIC_API_MOCKING=enabled
```

> Pastikan `.env.local` ada di `.gitignore`. Jangan commit flag ini ke repository.

### Aktivasi di Testing

Test setup mengimport `server` langsung dari `@/mocks/node` untuk kontrol penuh atas lifecycle:

```ts
// vitest.setup.ts
import { server } from "@/mocks/node"

beforeAll(() => server.listen())
afterEach(() => server.resetHandlers())
//                ↑ reset setelah setiap test — handler override per test tidak bocor
afterAll(() => server.close())
```

---

## Logging

`attachLogger()` di `src/mocks/index.ts` mendengarkan dua lifecycle event MSW dan mencetak log ke console:

| Event | Format | Keterangan |
|-------|--------|------------|
| `response:mocked` | `[MSW][BROWSER] GET /api/users → 200` | Request berhasil di-intercept |
| `request:unhandled` | `[MSW][SERVER] ⚠ unhandled: DELETE /api/users/1` | Request tidak punya handler — lewat ke server |

Log ini hanya muncul saat `NEXT_PUBLIC_API_MOCKING=enabled` karena MSW tidak aktif di luar kondisi itu. Tidak perlu matikan secara manual.

---

## Pola Penggunaan

### Endpoint Registry — `EP_`

Setiap feature yang memiliki API mendefinisikan path-nya di file `[feature].endpoint.ts`:

```ts
// api/user-management/user-management.endpoint.ts

export const EP_UserManagement = {
    list: "/api/user-management/users",
    detail: (id: string) => `/api/user-management/users/${id}`,
    create: "/api/user-management/users",
    delete: (id: string) => `/api/user-management/users/${id}`,
}
```

`EP_` adalah satu-satunya source of truth untuk path API di feature tersebut. Fungsi fetch dan mock handler keduanya import dari sini.

### Mock Handler — `[resource].mock-handler.ts`

Handler didefinisikan di file terpisah, co-located dengan file API-nya:

```ts
// api/user-management/users-list.mock-handler.ts

import { http, HttpResponse } from "msw"
import { EP_UserManagement } from "./user-management.endpoint"
import type { IRs_UserList } from "./users.type"

export const userListHandler = http.get(EP_UserManagement.list, () => {
    return HttpResponse.json<IRs_UserList>({
        items: [
            { id: "1", name: "Andikha", email: "andikha@example.com", role: "admin" },
            { id: "2", name: "Budi", email: "budi@example.com", role: "viewer" },
        ],
        total: 2,
    })
})

export const userCreateHandler = http.post(EP_UserManagement.create, async ({ request }) => {
    const body = await request.json() as { name: string; email: string; role: string }
    return HttpResponse.json({
        ok: true,
        data: { id: crypto.randomUUID(), ...body },
        message: "User berhasil dibuat",
    }, { status: 201 })
})
```

### Override Handler per Test

Untuk mensimulasikan error atau kondisi khusus di test tertentu:

```ts
// app/user-management/$test/user-management.integration.test.ts
import { server } from "@/mocks/node"
import { http, HttpResponse } from "msw"
import { EP_UserManagement } from "@/api/user-management/user-management.endpoint"

test("menampilkan pesan error saat API gagal", async () => {
    server.use(
        http.get(EP_UserManagement.list, () => {
            return HttpResponse.json(
                { message: "Internal Server Error" },
                { status: 500 }
            )
        })
    )

    // ... render dan assert error state
})
```

---

## Aturan Konvensi

- `EP_` adalah satu-satunya tempat mendefinisikan path API — tidak ada string path di `APIS_`, `APIC_`, maupun handler
- `[resource].mock-handler.ts` selalu import path dari `[feature].endpoint.ts` — tidak pernah hardcode
- Response mock **harus** sesuai dengan shape `IRs_` yang sudah didefinisikan — bukan karangan bebas
- `src/mocks/` hanya boleh berisi wiring (setup + import handler) — definisi handler tetap di `api/[feature]/`
- MSW **tidak pernah aktif di production** — selalu guard dengan `process.env.NEXT_PUBLIC_API_MOCKING`
- Setelah backend API siap, hapus file `mock-handler` terkait dan bersihkan import di `src/mocks/index.ts`

---

## Referensi

- [MSW Documentation](https://mswjs.io)
- [Pattern I — API Mocking with MSW](/patterns/api-mocking)
- [API Layer — EP_ dan mock-handler](/structure/api)
- [Symbol Naming — EP_ prefix](/naming/symbol)
